import { ComponentFixture, TestBed } from '@angular/core/testing';

import { SitePackagesComponent } from './site-packages.component';

describe('SitePackagesComponent', () => {
  let component: SitePackagesComponent;
  let fixture: ComponentFixture<SitePackagesComponent>;

  beforeEach(async () => {
    await TestBed.configureTestingModule({
      declarations: [ SitePackagesComponent ]
    })
    .compileComponents();

    fixture = TestBed.createComponent(SitePackagesComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
